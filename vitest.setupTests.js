// vitest.setup.js
import '@testing-library/jest-dom';
import { beforeEach, afterEach } from 'vitest'; // Using Vitest-specific hooks

beforeEach(() => {
  // Clear mocks or setup state before each test
});

afterEach(() => {
  // Cleanup after each test
});
